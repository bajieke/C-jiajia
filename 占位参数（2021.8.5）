#include<iostream>
using namespace std;


//占位参数: 只写一个类型，没有变量
//占位参数也可以有默认的参数值
void func(int a, int =100)
{
	cout << "this is func() " << endl;
}

int main()
{

	func(10,20);

	func(29);

	system("pause");
	return 0;
}
