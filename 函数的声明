#include<iostream>
using namespace std;

//函数可以声明多次，定义只一次

int max(int, int);

int main()
{
	int a = 10, b = 20;

	cout << max(a, b) << endl;

	return 0;
}

int max(int x, int y)
{
	return x > y ? x : y;
}
